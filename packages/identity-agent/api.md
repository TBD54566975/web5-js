## API Report File for "@web5/identity-agent"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import type { AppDataStore } from '@web5/agent';
import { DidManager } from '@web5/agent';
import type { DidRequest } from '@web5/agent';
import { DidResolver } from '@web5/dids';
import type { DidResponse } from '@web5/agent';
import { DwnManager } from '@web5/agent';
import type { DwnResponse } from '@web5/agent';
import { IdentityManager } from '@web5/agent';
import { KeyManager } from '@web5/agent';
import type { ProcessDwnRequest } from '@web5/agent';
import type { ProcessVcRequest } from '@web5/agent';
import type { SendDwnRequest } from '@web5/agent';
import type { SendVcRequest } from '@web5/agent';
import type { SyncManager } from '@web5/agent';
import type { VcResponse } from '@web5/agent';
import type { Web5ManagedAgent } from '@web5/agent';
import type { Web5Rpc } from '@web5/agent';

// @public (undocumented)
export class IdentityAgent implements Web5ManagedAgent {
    constructor(options: IdentityAgentOptions);
    // (undocumented)
    agentDid: string;
    // (undocumented)
    appData: AppDataStore;
    // (undocumented)
    static create(options?: Partial<IdentityAgentOptions>): Promise<IdentityAgent>;
    // (undocumented)
    didManager: DidManager;
    // (undocumented)
    didResolver: DidResolver;
    // (undocumented)
    dwnManager: DwnManager;
    // (undocumented)
    firstLaunch(): Promise<boolean>;
    // (undocumented)
    identityManager: IdentityManager;
    initialize(options: {
        passphrase: string;
    }): Promise<void>;
    // (undocumented)
    keyManager: KeyManager;
    // (undocumented)
    processDidRequest(request: DidRequest): Promise<DidResponse>;
    // (undocumented)
    processDwnRequest(request: ProcessDwnRequest): Promise<DwnResponse>;
    // (undocumented)
    processVcRequest(_request: ProcessVcRequest): Promise<VcResponse>;
    // (undocumented)
    rpcClient: Web5Rpc;
    // (undocumented)
    sendDidRequest(_request: DidRequest): Promise<DidResponse>;
    // (undocumented)
    sendDwnRequest(request: SendDwnRequest): Promise<DwnResponse>;
    // (undocumented)
    sendVcRequest(_request: SendVcRequest): Promise<VcResponse>;
    // (undocumented)
    start(options: {
        passphrase: string;
    }): Promise<void>;
    // (undocumented)
    syncManager: SyncManager;
}

// @public (undocumented)
export type IdentityAgentOptions = {
    agentDid: string;
    appData: AppDataStore;
    didManager: DidManager;
    didResolver: DidResolver;
    dwnManager: DwnManager;
    identityManager: IdentityManager;
    keyManager: KeyManager;
    rpcClient: Web5Rpc;
    syncManager: SyncManager;
};

// (No @packageDocumentation comment for this package)

```
